export const intradataConfig = {
  protocol: "https",
  url: "localhost",
  basePath: "v1",
  port: "8889",
};

export const baseUrl = `${intradataConfig.protocol}:${intradataConfig.url}/${intradataConfig.basePath}`;
